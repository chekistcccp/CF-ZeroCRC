[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cfzerocrc"
version = "0.1.0"
description = "Counterfactual SD3.5 flow localization for colorectal CT"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
